
import 'package:flutter/material.dart';

class SeachsPage extends StatelessWidget {
  const SeachsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Search"));
  }
}
